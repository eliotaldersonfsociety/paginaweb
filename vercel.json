{
  "version": 2,
  "builds": [
    { "src": "front/package.json", "use": "@vercel/static-build" },
    { "src": "api/**/*.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "front/$1" },
    { "src": "/api/(.*)", "dest": "api/$1" }
  ]
}

